// import Cookie from 'js-cookie'
/* eslint-disable no-unused-vars */
/*
export default function ({ app, $moment, $vuetify }) {
  const locale = Cookie.get('syp_language') || 'es'

  $moment.locale(locale === 'en' ? 'en-in' : locale)
  $vuetify.lang.current = locale

  app.i18n.onLanguageSwitched = (oldLocale, newLocale) => {
    Cookie.set('syp_language', newLocale)
    $moment.locale(newLocale === 'en' ? 'en-in' : newLocale)
    $vuetify.lang.current = newLocale
  }
}
*/
